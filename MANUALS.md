# Version Control GIT

```sh
git add .           # Add files to send
git commit -m "Update: ..." # Setup files to send
git push            # Push new changes to remote Server
```

```sh
git pull            # Pull new changes to local PC
```